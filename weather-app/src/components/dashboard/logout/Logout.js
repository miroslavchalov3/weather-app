import React, { Component } from 'react';

class Logout extends Component{

    state = {
    }


    render(){
        return (
            <button> Clic for log Out</button>
        )
    }
}

export default Logout;
